import * as React from "react";
import styled from "styled-components";
import { space, color } from "styled-system";

type Props = any;

export const @Sample = styled.div<Props>`
  ${space}
  ${color}
`;