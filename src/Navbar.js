import * as React from 'react';

import styled from 'styled-components';

const Wrapper = styled.section`
  padding: 4em;
  background: papayawhip;
`;

const Navbar = (props) => {
  return (
    <Wrapper className="fixed-bottom p-3 col">TEST</Wrapper>
  );
}

export default Navbar;