import * as React from "react";
import styled from "styled-components";
import { space, color } from "styled-system";
import { Flex } from "../atoms/Flex";

type Props = any;

export class SalesboostHeader extends React.Component<Props, any> {
  public render() {
    return (
      <div></div>
    );
  }
}